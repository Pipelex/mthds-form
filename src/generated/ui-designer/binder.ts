// >>> pipelex-codegen-stamp >>>
// crate_fingerprint: f69591b92d3374da7276177e4c81da30f7033be6c4c6e1561c6ca1398c7bd343
// engine_version: 0.57.0
// projection: types / ts-zod
// options: {}
// content_hash: 0be52a6f567687870039fd7cdc36ff301ea313faf96a6a0a401f33a19999504b
// <<< pipelex-codegen-stamp <<<
// ---------------------------------------------------------------------------
// AUTOGENERATED by Pipelex codegen — DO NOT EDIT.
//
// Typed parse/serialize helpers over the pure `types.ts` schemas. Field keys are
// wire-native (snake_case), so parse/serialize validate the wire payload directly.
//
// projection: types / ts-zod (binder)
// ---------------------------------------------------------------------------
import {
  ActionSchema,
  type Action,
  CatalogSchema,
  type Catalog,
  ComponentSchema,
  type Component,
  PagePlanSchema,
  type PagePlan,
  PlannedSectionSchema,
  type PlannedSection,
  TextSchema,
  type Text,
} from "./types";

/** Parse a wire payload into a validated `Action`. */
export function parseAction(wire: unknown): Action {
  return ActionSchema.parse(wire);
}

/** Validate a `Action` for the wire (keys are already wire-native). */
export function serializeAction(value: Action): Action {
  return ActionSchema.parse(value);
}

/** Parse a wire payload into a validated `Catalog`. */
export function parseCatalog(wire: unknown): Catalog {
  return CatalogSchema.parse(wire);
}

/** Validate a `Catalog` for the wire (keys are already wire-native). */
export function serializeCatalog(value: Catalog): Catalog {
  return CatalogSchema.parse(value);
}

/** Parse a wire payload into a validated `Component`. */
export function parseComponent(wire: unknown): Component {
  return ComponentSchema.parse(wire);
}

/** Validate a `Component` for the wire (keys are already wire-native). */
export function serializeComponent(value: Component): Component {
  return ComponentSchema.parse(value);
}

/** Parse a wire payload into a validated `PagePlan`. */
export function parsePagePlan(wire: unknown): PagePlan {
  return PagePlanSchema.parse(wire);
}

/** Validate a `PagePlan` for the wire (keys are already wire-native). */
export function serializePagePlan(value: PagePlan): PagePlan {
  return PagePlanSchema.parse(value);
}

/** Parse a wire payload into a validated `PlannedSection`. */
export function parsePlannedSection(wire: unknown): PlannedSection {
  return PlannedSectionSchema.parse(wire);
}

/** Validate a `PlannedSection` for the wire (keys are already wire-native). */
export function serializePlannedSection(value: PlannedSection): PlannedSection {
  return PlannedSectionSchema.parse(value);
}

/** Parse a wire payload into a validated `Text`. */
export function parseText(wire: unknown): Text {
  return TextSchema.parse(wire);
}

/** Validate a `Text` for the wire (keys are already wire-native). */
export function serializeText(value: Text): Text {
  return TextSchema.parse(value);
}
